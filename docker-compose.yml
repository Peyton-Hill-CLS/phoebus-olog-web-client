version: '3'
services:
  web:
    build: .
    ports:
      - "443:443"
    volumes:
      - /etc/letsencrypt/live/zerossl/cert.pem:/cert.pem
      - /etc/letsencrypt/live/zerossl/privkey.pem:/privkey.pem

